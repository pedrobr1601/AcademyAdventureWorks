version: 2

sources:
  - name: erpnorthwind
    tables:
      - name: public_orders
      - name: public_customers 
        description: essa é a tabela de clientes
        columns:
          - name: customer_id
            description: id do cliente
            tests:
              - unique
              - not_null